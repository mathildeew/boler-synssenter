import articles from "./article-schema";
import siteSettings from "./site-settings";
import hero from "./hero-schema";
import about from "./about-schema";

const schemas = [articles, siteSettings, hero, about];

export default schemas;
